version: '3.8'

services:
  webserver:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: nginx-website
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html
    restart: unless-stopped
